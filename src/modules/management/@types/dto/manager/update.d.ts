import type { Uuid } from '@types';

// * ---------------------------------------------------------------------- * //

export type ManagerUpdateData = {
  id: Uuid;
  name?: string;
  surname?: string;
  password?: string;
}

// * ---------------------------------------------------------------------- * //
