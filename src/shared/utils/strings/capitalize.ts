// * ---------------------------------------------------------------------- * //

function capitalize(str: string): string {
  return str.replace(/^\w/, c => c.toUpperCase());
}

// * ---------------------------------------------------------------------- * //

export { capitalize };
