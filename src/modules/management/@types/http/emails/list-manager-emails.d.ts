import type { Uuid } from '@types';

// * ---------------------------------------------------------------------- * //

export interface ListManagerEmailsRequestBody {
  userId: Uuid;
}

// * ---------------------------------------------------------------------- * //
