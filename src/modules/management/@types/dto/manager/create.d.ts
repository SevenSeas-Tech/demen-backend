// * ---------------------------------------------------------------------- * //

export type ManagerCreationData = {
  name: string;
  lastName: string;
  password: string;
  isActive?: boolean;
  email:string;
  emailType: string;
}

// * ---------------------------------------------------------------------- * //
