import type { Uuid } from '@types';

// * ---------------------------------------------------------------------- * //

export interface ListEmailsQuery {
  userId: Uuid;
}

// * ---------------------------------------------------------------------- * //
