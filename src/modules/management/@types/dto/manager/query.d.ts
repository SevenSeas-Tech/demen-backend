// * ---------------------------------------------------------------------- * //

export type ManagerQueryOptions = {
  emails: boolean;
  issues: boolean;
  phones: boolean;
  tokens: boolean;
  videos: boolean;
}

// * ---------------------------------------------------------------------- * //
